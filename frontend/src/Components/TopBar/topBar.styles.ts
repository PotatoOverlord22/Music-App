import { makeStyles } from '@mui/styles';

export const useTopBarStyles = makeStyles({
    topBar: {
        display: 'flex',
        flexDirection: 'row',
        alignContent: 'center',
        justifyContent: 'right',
        gap: '26px',
        marginLeft: '26px',
    }
});